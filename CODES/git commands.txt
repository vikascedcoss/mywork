OPEN THE LOCATION OF YOUR FOLDER WHICH IS TO BE PUSHED ON GIT
RIGHT CLICK AND SELECT OPEN GIT BASH HERE

git init
git add .
git commit -am "inital commit"

NOTE:- 
set user email and username if it asks like
git config --global user.email example@gmail.com
git config --global user.user.name firstnamesecondname

$ git remote add origin https://github.com/cedcoss/vikas-temp-training.git
$ git push -u origin master

